"""Custom message-based Strategy implementations for the three algorithms that never transmit
model parameters (SSFL/FD/DS-FL). FL reuses ``flwr.serverapp.strategy.FedAvg`` directly -- see
``server_app.py``.
"""
